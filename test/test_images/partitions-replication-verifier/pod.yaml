apiVersion: v1
kind: Pod
metadata:
  name: partitions_replication_verifier
spec:
  containers:
    - name: partitions_replication_verifier
      image: ko://knative.dev/eventing-kafka-broker/test/test_images/partitions-replication-verifier
